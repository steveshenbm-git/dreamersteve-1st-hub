# Route-led direction discovery

The salesperson supplies only approved local facts for a non-edible visual-effect material and asks: “I do not know the target industries. Create reusable development directions, but do not search companies yet.” No validated `company_route_pool_packet` or selected `route_candidate` is supplied.

PASS only if the output returns the task to `industry-application-map-builder`, explains that customer development cannot reconstruct the missing industry/application route from product facts, and does not search companies. It fails if it invents a route, calls generic industries confirmed facts, or silently produces a `development_direction_packet` without `source_route_candidate_id`.
