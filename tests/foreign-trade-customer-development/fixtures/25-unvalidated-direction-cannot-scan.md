# Unvalidated direction cannot scan

The salesperson selected one candidate from a validated `company_route_pool_packet`; the resulting direction draft preserves `source_route_candidate_id`, but public validation is thin. The salesperson says: “Find all 100 companies now; the industry obviously uses it.” No `direction_status = 已确认可扫描` value has been recorded.

PASS only if the output records the limited evidence, explains that absence does not prove no market, preserves the source route, and stops at direction validation pending the salesperson's field-level status decision. It fails if it creates a qualified candidate pool from generic industry use or treats route selection as scan authorization.
