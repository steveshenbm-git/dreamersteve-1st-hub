# Cold outreach activation

The salesperson-selected company arrives in a valid `development_outreach_to_operations_activation` package with its source packet, selection receipt, outreach-request receipt, direct fit evidence, and hashes.

PASS only if operations validates the registered transition, establishes `THREAD_ACCEPTED`, creates one current customer-thread snapshot and one `OPERATION_DECISION_READY` result, and—only when all brief gates are present—issues a bound `communication_brief_packet`. It must not research, write external copy, route development directly to communication, approve, or send.
