# Model A Screening

Read only the frozen product-neutral research theme, taxonomy node, official description, parent path, retrieval rule, and source scope. Run both required query groups. Inspect the first five distinct available results in each group, or all results when fewer than five exist. Open potential clues under the frozen limit. Return one screening record; never return an industry exclusion or `supported` evidence.
