# [COMPANY_NAME] 产品知识库规则

- 普通任务必须先确认 `company_id=[COMPANY_ID]`。
- 只能在本目录读取和写入 `[COMPANY_ID]` 的业务事实。
- 未确认 `company_id` 时停止读取业务资料并请求确认。
- 原始来源只追加封存，不覆盖、不改写；哈希不一致时停止正常使用并报告。
- 新来源衍生事实默认 E1；不得自动升级 E2/E3。
- E3 只允许审核通过的自有公司事实；E2 仅内部；E1 仅核实；E0 仅风险复核。
- 客户名单、行业路线、邮件、临时分析和 AI 推断不得自动进入事实库。
- 价格、库存、MOQ、交期、证书、法规、HS/海关、产能和客户案例缺失时必须保留缺口。
